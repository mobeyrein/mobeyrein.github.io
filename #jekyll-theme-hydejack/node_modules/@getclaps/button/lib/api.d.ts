import { UUID } from 'uuid-class';
export declare const getClaps: (href: string, parentHref: string, referrer: string) => Promise<{
    claps: number;
}>;
export declare const mine: (claps: number, href: string) => Promise<{
    href: string;
    id: UUID;
    nonce: number;
}>;
export declare const updateClapsApi: (claps: number, href: string, parentHref: string, id: UUID, nonce: number) => Promise<{
    claps: number;
}>;
export declare const cleanUp: (parentHref: string) => void;
//# sourceMappingURL=api.d.ts.map